plan_id: export-doc-sync-20260324
objective: Synchronize the two export-related documentation files with the behavior already implemented in src/Script/export.ks, using the script as the source of truth and preserving prior research constraints on unsupported parameters.
tasks:
  - id: sync-export-docs-to-implemented-behavior
    owner: implementer
    goal: Update src/Script/export.md and src/Script/rocket_modeling_required_parameters.md so they accurately describe the JSON fields, obtainable parameters, and documented limits of the current src/Script/export.ks implementation.
    deps: []
    status: pending
    inputs:
      - src/Script/export.ks
      - src/Script/export.md
      - src/Script/rocket_modeling_required_parameters.md
      - Prior research outcome that exact length, diameter, reference_area, drag_coefficient, payload_mass, and high-confidence deltaV are not safely obtainable from base kOS APIs and should remain Python-side computations seeded by exported parameters.
    outputs:
      - Updated src/Script/export.md aligned to the current export.ks behavior.
      - Updated src/Script/rocket_modeling_required_parameters.md aligned to the current export.ks behavior and prior research limits.
    acceptance:
      - Both documentation files match the fields and structure actually exported by src/Script/export.ks.
      - The documentation does not claim that exact length, diameter, reference_area, drag_coefficient, payload_mass, or high-confidence deltaV are directly obtainable from base kOS APIs.
      - The documentation clearly states that Python-side computation should use exported seed parameters where direct kOS export is unsafe or unavailable.
      - Documentation changes remain limited to synchronization with implemented behavior and previously established research findings.
    constraints:
      - Keep the plan minimal and dependency-aware.
      - Use src/Script/export.ks as the only implementation source of truth for current behavior.
      - Assign the documentation edit to owner implementer because the documentation-writer route was previously blocked by mode constraints.
      - Do not introduce speculative fields, unsupported guarantees, or implementation changes in this plan.