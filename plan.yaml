# plan.yaml
# Generated using plan_format_guide

objective: "Add export.ks script to vessel directory to output vessel telemetry as JSON"

waves:
  - name: "wave1"
    tasks:
      - id: "task1"
        name: "Implement export.ks script"
        role: "gem-implementer"
        description: |
          Create a new kOS script under `vessle/` named `export.ks`.
          The script should collect vessel telemetry (position, velocity,
          attitude, etc.) and format it as JSON for output.
        dependencies: []
      - id: "task2"
        name: "Test and validate export.ks"
        role: "gem-implementer"
        description: |
          Write tests or validation procedures to ensure the telemetry
          JSON output is correct and the script works in various flight
          scenarios. Verify integration with existing systems.
        dependencies:
          - "task1"
